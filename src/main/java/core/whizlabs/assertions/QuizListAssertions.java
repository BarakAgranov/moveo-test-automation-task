package core.whizlabs.assertions;

import core.BaseAssertions;
import core.whizlabs.elements.QuizListElements;

public class QuizListAssertions extends BaseAssertions<QuizListElements> {

//    public WhizlabsPracticeTestsAssertions free(String expectedTitle) {
//        Assert.assertTrue(elements().freeTest().get(0).getText().contains(expectedTitle), "The form's title doesn't contain the specified text.");
//        return this;
//    }
//
//    public WhizlabsPracticeTestsAssertions practice(String expectedTitle) {
//        Assert.assertTrue(elements().practiceTest().get(0).getText().contains(expectedTitle), "The form's title doesn't contain the specified text.");
//        return this;
//    }
}
